刚刚这一轮更新后，整体判断确实需要改。现在已经不是“P4.2 值不值得继续”的问题，而是：

[
\boxed{\text{P4.2 已经成功关闭原先最关键的统计不确定性}}
]

但与此同时，它暴露出下一阶段几个比继续调 (\lambda) 更重要的问题：**主文档已经严重滞后、强 Gate 的论文表述需要收紧、跨几何泛化还没闭合、控制总线仍未进入真实通信资源约束、CA 的理论桥还没有完全跟上实现。**

我按最新 GitHub `main` 到 **`c522006`** 做了重新审计。

------

# 1. 最新状态：这次结果是实质性升级

最新链条是：

[
8d5ddb8
\rightarrow
2e37494
\rightarrow
dbbcc2c
\rightarrow
273257e
\rightarrow
c522006
]

其中最关键的是：

- `2e37494`：实现 P4.2 anytime-valid QoS CS；
- `273257e`：清除 stale P3.4 结果引用并修正文档；
- `c522006`：在 **clean HEAD `273257e`** 上重新正式运行，结果再次复现。

最终结果文件明确记录：

[
\text{git_commit}=273257e,
\qquad
\text{git_dirty}=false
]

并且：

[
\boxed{
\text{CA=PASS},\qquad
\text{v2=FAIL}
}
]

发生在 60,000 episodes。

所以这一轮不是 smoke，也不是 dirty-tree 数字，而是真正可冻结的正式结果。`c522006` 本身也只是 result-only clean rerun，符合目前的 provenance 规范。

------

# 2. P4.2 的统计实现，我认为总体是正确的

这一点先给明确结论：

[
\boxed{\text{P4.2 statistical core = PASS}}
]

实现已经不是简单的 4 次 Clopper–Pearson + alpha spending，而是 Beta mixture e-process：

# [ M_n(p)

\frac{
B(S_n+a,n-S_n+b)
}{
B(a,b)p^{S_n}(1-p)^{n-S_n}
},
\qquad
a=b=\frac12.
]

然后通过

[
C_n=
{p:M_n(p)<1/\delta_s}
]

构造 time-uniform confidence sequence。

代码中确实：

- 在 log-domain 计算；
- 使用 `betaln` 防数值下溢；
- 用 root finding 反演 interval；
- 每 stream 使用
  [
  \delta_s=\frac{0.05}{32}=0.0015625;
  ]
- 32 个 stream 是
  (2) algorithms × (8) targets × FA/MD；
- union bound 不依赖各 stream 独立，因此 CRN 不会破坏 familywise validity。

这部分数理逻辑没有发现方向性错误。

------

# 3. fresh certification 也真正解决了 post-selection 问题

这是上一轮最担心的问题之一，现在处理得比较好。

P4.1b 用旧数据发现：

[
\text{geom 2 + congested}
]

是 boundary cell。

P4.2 没有继续复用那些随机样本，而是采用：

[
\text{cert seed}=100000+mc
]

的新 namespace。

代码明确冻结：

- geometry = 2；
- (\rho=1.8)；
- threshold；
- scheduler；
- price bits；
- maximum episode budget；

然后新生成 certification stream。

因此现在可以很干净地说：

> P4.1b 是 discovery，P4.2 是 independent conditional confirmation。

这一点相比之前可信度提升非常明显。

------

# 4. 新结果本身比“CA PASS / v2 FAIL”还更有信息

详细看各阶段：

### 7.5k episodes

[
\text{v2=UNCERTAIN},\qquad
\text{CA=UNCERTAIN}.
]

### 15k episodes

仍然：

[
\text{v2=UNCERTAIN},\qquad
\text{CA=UNCERTAIN}.
]

### 30k episodes

变成：

[
\boxed{
\text{v2=FAIL},\qquad
\text{CA=UNCERTAIN}
}
]

v2 的 target 0 FA lower bound 已经达到：

# [ L_{\rm FA,0}^{v2}

0.051648>0.05.
]

也就是说 v2 的失败不是 60k 才“偶然出现”，而是在 30k 时已经越过 time-uniform failure boundary。

### 60k episodes

最终：

# [ L_{\rm FA,0}^{v2}

0.051468>0.05
]

而 CA 最危险的 target 0：

# [ U_{\rm FA,0}^{CA}

0.048235<0.05.
]

因此：

[
\boxed{
\text{FAIL(v2)}
\land
\text{PASS(CA)}
}
]

成立。

------

# 5. 更有意思的是：point estimate 也不是擦边相等

60k 时 target 0 的实际 FA point estimate 是：

# [ \widehat P_{\rm FA,0}^{v2}

\frac{1741}{30132}
\approx0.05778,
]

CA：

# [ \widehat P_{\rm FA,0}^{CA}

\frac{1276}{30132}
\approx0.04235.
]

两者差：

[
\boxed{
1.54\text{ percentage points}
}
]

并不是 (0.0501) 对 (0.0499) 那种纯统计边界现象。

更重要的是：

[
\text{v2 CI lower}=0.05147,
]

[
\text{CA CI upper}=0.04823.
]

两个 simultaneous/time-uniform 区间甚至已经在 (0.05) 两侧**完全分离**。

这是相当漂亮的结果。

------

# 6. 性能结果也被 fresh sample 再次重复了

P4.1b 的 geom2 congested 是：

[
J_{v2}\approx5.101,
\qquad
J_{CA}\approx3.680.
]

约：

[
27.9%
]

delay improvement。

P4.2 的全新 60k certification stream 得到：

[
J_{v2}=5.0728,
\qquad
J_{CA}=3.6935.
]

即：

[
\frac{5.0728-3.6935}{5.0728}
\approx
\boxed{27.2%}.
]

虽然 P4.2 的 primary endpoint 是 QoS certification，不是 delay performance inference，但从机制复现角度，这是非常好的交叉验证：**新的随机样本并没有把原来的性能优势洗掉。**

------

# 7. 但是必须马上纠正一个潜在的论文过度表述

这里是这次深审最重要的概念问题。

当前可以严格说：

[
\boxed{
\text{registered v2 configuration is QoS-infeasible}
}
]

但还不能直接说：

[
\boxed{
\text{FRIDS-v2 algorithm itself is fundamentally infeasible}
}
]

原因是当前 P4.2 **冻结了同一套 stopping thresholds**。

runner 自己写得很明确：这是

> same stopping policy for both schedulers

用来隔离 scheduler 差异。

这是一种完全合理的**机制比较实验**。

但是 reviewer 很可能会问：

> 如果允许 FRIDS-v2 单独重新校准 upper/lower thresholds，它还能否满足 (P_{FA},P_{MD}\le0.05)？

例如单纯提高 v2 upper threshold，理论上就可能降低其 FA，只不过：

- delay 会增加；
- MD 可能变化；
- worst-target delay 会恶化。

所以当前 strong Gate 真正证明的是：

> **在冻结、共同的 detection policy-B operating point 和相同通信容量模型下，CA 满足 QoS，而 FRIDS-v2 违反 QoS。**

这个已经很强。

但不能翻译成：

> FRIDS-v2 在这个物理场景下不存在任何可行的 QoS operating point。

那需要另一个实验。

------

# 8. 因此我建议补一个非常小、但论文价值很高的 P4.2b

名称可以叫：

[
\boxed{
\text{QoS-Matched Operating-Point Frontier}
}
]

不要改 scheduler。

只允许两者分别调 stopping thresholds：

[
(A_q,B_q)
]

在独立 calibration set 上寻找满足：

[
P_{FA,q}\le0.05,
\qquad
P_{MD,q}\le0.05.
]

然后在完全 held-out test stream 上比较：

[
J_{\rm QoS}^{CA}
\quad\text{vs}\quad
J_{\rm QoS}^{v2}.
]

最终会出现两种结果。

### Case A：v2 找不到可行阈值

那才可以真正升级成：

[
\boxed{
\text{CA feasible while v2 infeasible}
}
]

### Case B：v2 也可以重新校准到 PASS

那论文结论转成更成熟的：

[
\boxed{
J_{CA}^{QoS}
<
J_{v2}^{QoS}
}
]

即：

> CA achieves a lower stopping delay at matched certified QoS.

从审稿角度，我甚至认为 **Case B 也完全没问题**，因为这是最公平的 operating-point comparison。

------

# 9. 当前真正最大的 P0：主系统文档已经与代码严重脱节

这是这次“整体内容审计”发现的最大问题。

当前 `docs/SYSTEM_MODEL.md` 开头仍然写：

> fixed geometry … one fusion center

并继续使用：

- centralized report-set scheduling；
- BSC/erasure；
- expected-(P_D) fusion；
- RIS；
- G3/G4/G5；
- global bit budget；

它完全没有把现在真正的：

- distributed UAV owners；
- FRIDS-v2；
- Dual-Bus；
- task price；
- airtime price；
- owner-directed evidence；
- sequential LLR stopping；
- shared airtime admission；
- CA-FRIDS；

作为系统主模型。

换句话说：

[
\boxed{
\text{代码的主系统已经到了 P4.2，
但 SYSTEM_MODEL 仍基本停留在旧论文时代}
}
]

这已经不是一般 stale doc，而是**论文定义层面的冲突**。

如果现在投稿，reviewer 只看系统模型甚至无法推导你刚刚得到的 P4.2 结果。

------

# 10. README 的问题更加严重：它现在更像“研究历史仓库”

`docs/README.md` 的顶层定位仍是：

> exact selective soft-information fusion simulator

核心 contribution 仍写：

- post-communication correlated value；
- (P_D)-optimal fusion；
- exact budget/max-min selection；
- RIS instance。

但文档后面又连续追加：

- NOMP；
- MAPPO；
- multistatic；
- collision GLRT；
- F0；
- FRIDS；
- service-delay bridge；
- CA-FRIDS……

结果就是一个 140KB 级别的“研究演化日志”。

这会造成一个非常严重的问题：

[
\boxed{
\text{历史是完整的，但当前研究主线不清楚}
}
]

这正是你之前一直担心的“系统跑偏/内容发散”的文档版本。

现在算法反而已经开始收敛，文档却还没收敛。

------

# 11. `PROBLEMS_AND_OPTIMIZATION.md` 也明显过期

当前文件日期仍是 **2026-08-17**，核心残余问题仍写：

- F0；
- WTA/NOMP；
- MAPPO；
- conditional FRIDS；
- “FRIDS-v2 冻结”；
- “下一步是投递/协调层”。

但现在最关键的事实已经变成：

[
\boxed{
\text{CA-FRIDS 在注册 boundary cell 上：
QoS PASS + v2 FAIL}
}
]

所以当前整个问题清单需要重新排序。

------

# 12. FORMAL_PROOFS 也出现了“历史理论正确，但不是当前算法理论”的问题

这份文档本身质量不差，问题在于**理论主角已经变了**。

当前 formal chain 主要停在：

[
\text{FRIDS-v2}
]

Theorem 4.95 是 FRIDS-v2 的：

# [ J_{iq}

\frac{y_qg_{iq}}{D_q}.
]

Theorem 4.109 是 local-vs-common action invariance。

Theorem 4.110/4.111 是 FRIDS 的 service-delay bridge。

但最新主算法其实已经使用：

# [ \boxed{ J^{CA}_{iqa}

## \pi_q g_{iqa}

\lambda_{o(q)}c_{iqa}
}
]

配合：

[
\sum_i\tau_{ij}\le T_{\rm air}.
]

目前 `FORMAL_PROOFS.md` 并没有形成一个完整的：

[
\text{CA primal}
\rightarrow
\text{dual}
\rightarrow
\text{best response}
\rightarrow
\text{hard feasible admission}
\rightarrow
\text{sequential QoS}
]

新理论链。

------

# 13. 更严重的是，旧 airtime 理论和当前 hard admission 已经不是一个机制

现有 Proposition 4.102/4.103 还在写：

[
L_i>T_{\rm air}
]

以后使用：

[
\min(1,T_{\rm air}/L_i)
]

进行 overflow thinning。

但 P4.1b 已经把正式主线改成：

[
\boxed{
\text{offer}
\rightarrow
\text{hard admission}
\rightarrow
\text{link}
}
]

并严格保证：

[
\sum_i\tau_{ij}\le T_{\rm air}
]

pathwise。

这两种模型完全不同。

因此 Formal Proof 当前至少需要清晰标：

> Proposition 4.102/103 = historical G7 model, superseded for P4/CA-FRIDS.

否则一个 reviewer 会发现：

> theorem 写 overflow thinning，实验却用 hard admission。

这是典型的理论—代码不一致。

------

# 14. Congested Freedman bridge 的旧问题仍然存在

最新 P4.2 没有动 scheduler，这没问题。

但是当前 Theorem 4.110 写：

# [ \widetilde A_q(t)

\sum_{\tau,i}
x_{iq,\tau}\widetilde g_{iq,\tau}.
]

在拥塞 hard admission 下，真正 owner 收到的随机增量应该包含：

[
I_{iq,t}^{adm}
I_{iq,t}^{link}.
]

如果 (x_{iq,t}) 只是 sensing/scheduling action，那么

[
x_{iq,t}\widetilde g_{iq,t}
]

不能直接作为 admission 后的 predictable drift。

所以最新 P4.2 可以有：

[
\text{QoS empirical certificate}
]

但暂时不能把它与现有 Theorem 4.110 无缝拼成：

[
\text{CA theoretical delay guarantee}.
]

这一点仍然要修。

------

# 15. P4.2 runner 还有两个小的 provenance 问题

不影响 strong Gate 数值，但建议修掉。

## 问题 A：`incremental` 的含义不准确

stage=30k 时：

```python
incremental = inc_v2/inc_ca
```

实际上只记录**第 20 个 1500-episode cell**。

它并不是：

[
15k\rightarrow30k
]

这 15k 新样本的增量。

所以如果字段名意思是“since previous stage”，现在是错的。

建议改成：

```text
latest_cell
```

或者真正累加：

```text
stage_increment
```

------

## 问题 B：stage fingerprint 只记录最后一个 cell tape

当前每次 stage 写：

```python
crn_tape_fingerprint = fp
```

而 `fp` 只是当前：

[
\text{cert_seed}+mc
]

那一个 1500-run tape 的 fingerprint。

但 cumulative stage 已经包含之前所有 cells。

因此这个 fingerprint 不能单独重建 30k/60k prefix。

建议维护：

[
h_t=
H(h_{t-1}\Vert fp_t)
]

的 cumulative hash chain，或者直接保存 40 个 cell fingerprints。

这属于：

[
\boxed{\text{P1 credibility polish}}
]

不是结果无效。

------

# 16. P4.2 的测试很好，但还有一个值得补的真正 sequential test

现在增加的 11 个测试包括：

- endpoint；
- empirical rate containment；
- sample size tightening；
- confidence-level monotonicity；
- fixed-time coverage MC；
- PASS/FAIL/UNCERTAIN；
- familywise delta。

这些已经不错。

但是现在叫：

> anytime-valid

最好再加一个直接的 pathwise Monte Carlo test：

[
\Pr_p
\left(
\exists n\le N:
p\notin C_n
\right)
\le\delta
]

至少选：

[
p\in{0.01,0.05,0.1,0.5}.
]

这不是理论必须靠 MC 证明，而是防止**实现上的 prefix/root bug**。

目前测试的 coverage simulation 只检查最终固定 (n)，还没有直接覆盖“ever crossing”。

------

# 17. 还有一个新的统计层面问题：P4.2 解决了一个 cell，不等于全系统 95% certificate

P4.2 的 32 streams familywise control 是：

[
\boxed{\text{geom2 / congested cell 内}}
]

非常严格。

但是 P4.1b 的另外 5 个 cell 是分别做 cell-level certification。

如果你现在把二者组合写成：

> all six scenarios are jointly certified with 95% confidence

就不完全对。

因为六个 cell 的总体 familywise error 并没有统一分配。

所以建议区分：

### 可以说

> CA passed all six registered benchmark cells; the previously unresolved boundary cell was independently resolved by a 32-stream anytime-valid 95%-familywise certificate.

### 暂时不要说

> The complete six-cell benchmark is jointly certified at 95% familywise confidence.

如果需要后一句，其实不难。

重新建立一个：

# [ 6 \times8 \times2

96
]

CA stream family，或者 CA/v2 一起：

[
192
]

streams，统一分配 familywise budget。

已有原始 counts 的 cell 甚至不一定要全部重跑。

------

# 18. 新结果现在是否足以把 CA-FRIDS 升为主算法？

我的判断和上一轮相比已经发生变化。

上一轮：

[
\boxed{\text{FRIDS-v2 frozen}}
]

是合理的。

现在：

- uncongested 不退化；
- congested mean gain +25.4%；
- 三 geometry 均显示方向性优势；
- 原来唯一 CA UNCERTAIN cell 已经 fresh-sample PASS；
- 该 cell 的 v2 反而 certified FAIL；
- fresh sample delay gain 仍约 27.2%。

因此现在我认为应该把项目状态改成：

[
\boxed{
\textbf{CA-FRIDS = registered-benchmark primary scheduler}
}
]

而：

[
\boxed{
\text{FRIDS-v2 = reference / ablation baseline}
}
]

但注意限定词：

**registered-benchmark**。

还不能说 CA 是：

> universally superior distributed UAV scheduler.

------

# 19. 不要修改历史 P4.1b JSON 的 `adopt_ca=false`

这一点很重要。

历史结果中：

```text
adopt_ca = false
```

当时是正确的，因为当时：

[
5PASS+1UNCERTAIN.
]

不要回头篡改它。

正确做法是新增一个：

[
\boxed{\text{P4-META / P4.2-closure certificate}}
]

明确引用：

[
\text{P4.1b clean result}
+
\text{P4.2 clean result}.
]

然后产生新的 project-level verdict：

```text
P4.1b:
  performance_gate = PASS
  qos = 5 PASS + 1 UNCERTAIN

P4.2:
  unresolved_cell = CA PASS
  v2 = FAIL

combined_registered_verdict:
  CA-FRIDS = PROMOTE
```

这样 provenance 最干净。

------

# 20. 算法创新定位现在也应该正式改变

现在越来越明确：

[
|\Delta_{\rm architecture}|
\gg
|\Delta_\lambda|

> 

|\Delta_{\rm admission}|.
]

所以论文不要再围绕：

> congestion-price scheduler

展开。

最稳的主贡献应该是：

[
\boxed{
\textbf{QoS-Certified Task-Oriented Dual-Bus Cooperative Detection}
}
]

结构是：

[
\text{Local sensing}
]

[
\downarrow
]

[
\text{task-price bus}
]

[
+
]

[
\text{receiver-capacity bus}
]

[
+
]

[
\text{owner-directed evidence bus}
]

[
\downarrow
]

[
\text{hard airtime-feasible reporting}
]

[
\downarrow
]

[
\text{sequential owner detection}
]

[
\downarrow
]

[
\text{anytime-valid QoS certificate}.
]

这比“又一个 primal-dual UAV scheduler”更完整。

------

# 21. 现在真正的通信物理第一短板：250 bit control plane 仍然是“账本存在、资源约束不存在”

CA 现在每周期大约需要：

# [ Qb_\pi+Kb_\lambda+b_Z

# 8(10)+16(10)+10

250
\text{ bit}.
]

但这个控制成本主要是 accounting，并没有与 evidence packet 一起进入：

[
T_{\rm frame}.
]

因此现在 evidence plane 有：

[
\sum\tau_{ij}\le T_{\rm air},
]

control plane 却仍然接近“逻辑广播”。

下一步物理闭环必须决定：

### 方案 A：正交 control/data

[
T_{\rm ctrl}\le T_{\rm ctrl}^{max},
\qquad
T_{\rm ev}\le T_{\rm ev}^{max}.
]

### 方案 B：统一 airtime

[
\boxed{
T_{\rm ctrl}
+
T_{\rm evidence}
\le
T_{\rm frame}
}
]

我更推荐 A 作为第一版，因为：

- 物理解释简单；
- 不会把整个 scheduler 再次重构；
- 可以直接研究 control-overhead scaling。

------

# 22. 随 UAV 数目增加，现在有一个非常清楚的理论可扩展问题

当前 control：

# [ B_{\rm ctrl}

Qb_\pi+Kb_\lambda+b_Z.
]

所以：

# [ B_{\rm ctrl}

O(K+Q).
]

这比 full-mesh evidence：

[
O(K^2)
]

已经好多了。

但随着 (K) 增加，(\lambda_j) bus 仍线性增长。

所以之前讨论的“计算机总线”思想在这里反而终于有了一个非常合理的落脚点：

> 不需要让每个 receiver 每周期广播完整价格；只有当价格变化足以改变本地 action 时才更新。

即：

[
m_i>2E_i
]

时保持旧价格即可。

这会自然形成：

[
\boxed{
\text{Certificate-Triggered Sparse Control Bus}
}
]

这比继续改 (\lambda) 更新公式创新性更强。

------

# 23. 但是跨几何泛化现在仍然是当前最大的“论文证据缺口”

现在：

[
100MC\times300
]

或者 60k episodes 很深。

但是 outer geometry：

[
G=3.
]

P4.2 也只是同一个 geometry 2 的新 MC。

所以你现在对“随机检测试验”的统计不确定性压得非常低，

但：

[
\boxed{
\text{geometry uncertainty 仍然很大}
}
]

这就是典型：

> inner MC 很深，outer environmental diversity 太浅。

下一阶段必须把算力从 inner MC 转向 outer geometry。

我建议不是再做：

[
3\times100MC\times1000
]

而是例如：

[
\boxed{
20\sim30\text{ held-out geometries}
}
]

每个 geometry 中等 MC。

这样回答的问题才是：

[
P_G
\left[
J_{CA}(G)<J_{v2}(G)
\right]
?
]

以及：

[
P_G[
CA\ QoS\ feasible
]
?
]

------

# 24. 建议现在重新定义优化优先级

我现在给出的优先顺序是：

| 优先级   | 内容                                    | 原因                                   |
| -------- | --------------------------------------- | -------------------------------------- |
| **P0**   | 当前文档统一 / meta verdict             | 系统模型已严重落后于代码               |
| **P0**   | 收紧“v2 infeasible”的表述               | 当前只证明 frozen operating point FAIL |
| **P1**   | QoS-matched threshold frontier          | 防 reviewer 最直接反驳                 |
| **P1**   | held-out geometry certification         | 当前最大的泛化缺口                     |
| **P1**   | control-bus physical closure            | 当前最大的通信物理缺口                 |
| **P1**   | CA admission-aware martingale bridge    | 当前最大的理论—实现缺口                |
| **P2**   | B00 architecture ablation               | 拆 owner routing / dynamic task price  |
| **P2**   | sparse certified control bus            | 下一阶段真正值得发展的新算法           |
| **停止** | 继续调 (\lambda)、density、更多小价格项 | 数据已经证明不是主收益源               |

------

# 25. 我建议把接下来的阶段重新命名，而不要继续无止境 P4.x

现在 P4 已经完成了它最核心的任务：

[
\boxed{
\text{shared-capacity}
+
\text{credible comparison}
+
\text{QoS certification}
}
]

所以建议：

### P4-CLOSE

做三件事：

1. `P4_META_CERT.json`
2. 更新 `SYSTEM_MODEL`
3. 更新 `FORMAL_PROOFS`/`README`/`RUN_MATRIX`

正式宣布：

[
\boxed{
\text{CA-FRIDS promoted on registered benchmark}
}
]

------

然后进入新的大阶段：

[
\boxed{\textbf{P5 — Deployment/Generalization Closure}}
]

只研究三个真正剩余的大问题：

### P5.1 — QoS-matched operating point

回答：

> v2 是真正不可行，还是仅 frozen threshold 不合适？

### P5.2 — Held-out geometry generalization

回答：

> CA 的优势是否跨环境存在？

### P5.3 — Physical + sparse control bus

回答：

> Dual-Bus 自己的控制通信成本是否仍保持 scalable？

这比继续 P4.3、P4.4、P4.5 一路加 patch 更容易保持理论收敛。

------

# 26. 当前完整科研结论

现在整个研究已经从早期：

[
\text{soft fusion}
\rightarrow
\text{selection}
\rightarrow
\text{NOMP/MAPPO}
\rightarrow
\text{resource allocation}
]

逐渐收敛到一个更明确的问题：

[
\boxed{
\begin{aligned}
&\text{How should distributed UAVs acquire and report}\
&\text{only task-relevant detection evidence}\
&\text{under hard U2U airtime constraints}\
&\text{while preserving sequential detection QoS?}
\end{aligned}
}
]

现在 CA-FRIDS 给出的回答已经相当清楚：

[
\boxed{
\text{owner-directed evidence}
+
\text{task-oriented prices}
+
\text{hard airtime admission}
+
\text{sequential detection}
}
]

而 P4.2 又进一步增加：

[
\boxed{
\text{anytime-valid statistical certification}.
}
]

这已经具备一个比原来“各种模块都优化一点”明显更收敛的论文主线。

------

## 最终审计结论

我现在会把整个系统状态定为：

[
\boxed{
\textbf{P4 algorithmic/statistical core = PASS}
}
]

但同时：

[
\boxed{
\textbf{documentation/theory/generalization closure = NOT YET PASS}
}
]

最新的 `CA PASS / v2 FAIL` **是真结果，而且 clean rerun 后稳定复现**；它足以让 CA-FRIDS 从“candidate”升级为**注册 benchmark 下的主 scheduler**。

但论文暂时不要写成“FRIDS-v2 fundamentally infeasible”。准确表述应是：

> **Under the frozen common stopping policy and matched shared-airtime capacity model, CA-FRIDS is anytime-valid QoS-certified feasible at the registered congested boundary cell, whereas FRIDS-v2 is certified to violate the false-alarm constraint.**

接下来最应该做的已经不是继续优化算法小参数，而是**先做一次 P4-CLOSE，把现在散落的代码、理论、SYSTEM_MODEL、历史 README 和最终 verdict 全部重新对齐；然后进入 P5，只解决“QoS-matched 公平性、跨几何泛化、控制总线物理闭环”三个真正的大问题。**